.wrapper_with_padding
    %h1 Notes
    #notes.clearfix
    -unless @notes.blank?
        -@notes.each do |note|
            %h2=link_to note.title, note_path(note.id)
            %p="#{time_ago_in_words(note.created_at)} - Owner: #{note.user.email}"  
        
    =link_to 'Create Note', new_note_path


